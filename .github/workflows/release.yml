name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  publish-npm:
    runs-on: ubuntu-latest
    name: Publish @opencoachingformat/renderer to npm
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Check tag matches package.json version
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          PKG_VERSION="v$(node -p "require('./package.json').version")"
          if [ "$VERSION" != "$PKG_VERSION" ]; then
            echo "::error::tag $VERSION does not match package.json version $PKG_VERSION"
            exit 1
          fi

      - run: npm ci
      - run: npx tsc --noEmit
      - run: npm test

      # Authenticates via OIDC trusted publishing (npm CLI >=11.5.1, bundled
      # with actions/setup-node@v6's Node 22) — no NPM_TOKEN needed. Requires
      # a trusted publisher already configured on the npm package pointing at
      # this repo + this workflow file, which in turn requires the package to
      # already exist (a one-time manual `npm publish` bootstraps it, which
      # runs prepublishOnly and rebuilds — dist/ doesn't need to exist here).
      # Provenance is automatic with trusted publishing, no --provenance flag.
      - name: Publish to npm
        run: npm publish
